{
  "name": "calistenia-adaptativa",
  "version": "0.1.0",
  "private": true,
  "description": "Motor de entrenamiento adaptativo de calistenia — FASE 1: el cerebro (datos + motor de progresion).",
  "type": "module",
  "scripts": {
    "scenario": "tsx src/scenarios/muscleup-user.ts",
    "check-data": "tsx src/scenarios/check-data-integrity.ts",
    "weekly-plan": "tsx src/scenarios/weekly-plan-demo.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
